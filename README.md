# Package Picker Optimizer

## Local preview

Run:

```bash
npm start
```

Then open:

```text
http://127.0.0.1:4173
```

Optional:

- Set `PORT` to use a different port.
- Set `HOST` to expose a different host address.
